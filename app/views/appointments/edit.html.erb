<%# edit page for appoinment this will render to appointment form page%>
<section class="service-sec mt-5">
        <div class="text-center mt-5">
                <p>مجمع آراك الرقمي لطب الأسنان</p>          
                <div class="card border-light mb-3 text-right">
                        <div class="card-body">
                                <div class="row mb-2">
                                    <div class="col ml-3 text-left">
                                          <%= link_to 'عرض', @appointment, class:"profile-edit-btn" %> |
                                          <%= link_to 'رجوع', appointments_path, class:"profile-edit-btn" %> </p>
                                    </div>
                                    <h5 class="text-center text-color mr-3">تعديل الموعد </h5>
                                 </div>
                                <p><%= render 'form', appointment: @appointment %></p>
                        </div>
                </div>
        </div>
</section>

